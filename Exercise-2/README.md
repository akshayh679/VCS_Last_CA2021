Exercise 2: Tracking New Files

In this exercise, I created new files and used `git status`
to check untracked files. I then added the files using `git add`
and committed them.

This demonstrates how Git tracks new files explicitly.
