Exercise 4: Branching

In this exercise, I created a new branch using `git branch`
and switched to it using `git checkout`. I made changes and
committed them on the feature branch.

This demonstrates working on a separate branch without affecting main.
