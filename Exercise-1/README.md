Exercise 1:  Git Initialization

In this exercise, I initialized a Git repository using `git init`,
added files using `git add`, and created the first commit using
`git commit`.

This exercise demonstrates the basic setup of a Git repository.
